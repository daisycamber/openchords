var lengthMeasures = []
function addLengthMeasures(){
  /*
  for(var i = 0; i < 8; i++) {
    lengthMeasures[i] = new createjs.Shape();
    lengthMeasures[i].graphics.beginFill("#000000").drawPolyStar(((stage.canvas.width-KEYSIZE)/8 * (i+1)) + KEYSIZE, KEYSIZE * 4.3, KEYSIZE/2, 3, 0, 90);
    lengthMeasures[i].addEventListener("mousedown", function(){
      console.log("test")
    })
    lengthMeasures[i].addEventListener("pressmove", function(event){
      mouseDown = false
      event.currentTarget.x = event.stageX - (stage.canvas.width-KEYSIZE)/8 - KEYSIZE
    })
    stage.addChild(lengthMeasures[i])
  }*/

}
